const { getSolutionPart1, getSolutionPart2 } = require('./solutions');

describe(('#getSolutionPart1'), () => {
  it('', () => {
    expect(getSolutionPart1()).toEqual(0);
  });
});

describe(('#getSolutionPart2'), () => {
  it('', () => {
    expect(getSolutionPart2([])).toEqual(0);
  });
});
